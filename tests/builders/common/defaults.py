"""
Common default values for test builders.

This module centralizes shared default values used by
test builders to avoid duplicated knowledge.
"""

from datetime import datetime

DEFAULT_TIMESTAMP = datetime(2026, 1, 1)

DEFAULT_SYMBOL = "TEST"

DEFAULT_TIMEFRAME = "1H"
